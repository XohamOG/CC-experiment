# Backend (Express)

Quickstart

1. cd into the Backend folder
2. Install deps: `npm install`
3. Start dev server: `npm run dev`

Default dev server: http://localhost:3000

API endpoints:
- GET / => { message: 'Hello from backend' }
- GET /api/hello => { msg: 'Hello from /api/hello' }
